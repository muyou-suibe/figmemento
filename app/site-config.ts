export const siteConfig = {
  // Working values only. These are intentionally centralized so the confirmed brand can replace them later.
  brandName: "PhotoGift",
  domain: "",
  supportEmail: "hello@photogift.example",
  shippingThreshold: 49,
  candidateCategories: ["3D keepsakes", "Pet memories", "Digital gifts"],
} as const;

